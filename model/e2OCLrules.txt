assignments start before the deadline


university must have a name
	invariant UniName:
			self.Name.size() > 0;


course ID must be unique:
	invariant uniqueID:
			Course.allInstances()->isUnique(ID);
			


persons must have a name
